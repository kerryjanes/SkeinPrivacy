# Rust / Anchor
.anchor
target
**/*.rs.bk

# Node / TS
node_modules
dist
.yarn
*.tsbuildinfo

# Local validators / test ledgers
test-ledger
test-ledger.log
.surfpool

# Deployment manifests (environment-specific outputs)
services/registry-provision/manifests/*.json
services/registry-provision/manifests/archive/*.json
.playwright-cli/

# Secrets & local config
.env
.env.*
!.env.example
keypairs/*.json
!keypairs/.gitkeep

# OS
.DS_Store

# Tauri generated
clients/desktop/src-tauri/gen

# Bundled sing-box sidecar binaries (fetched via scripts/fetch-singbox.sh)
clients/desktop/src-tauri/binaries/
public/
.vercel
.env*
.env*.local
/site/
/clients/web/
/vercel.json
/.vercelignore
scripts/build-web.sh
scripts/weft-node.sh
*.key
node.json
