/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findConfigPda, findPositionPda } from '../pdas';
import { STAKING_PROGRAM_ADDRESS } from '../programs';

export const REQUEST_UNSTAKE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  44, 154, 110, 253, 160, 202, 54, 34,
]);

export function getRequestUnstakeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REQUEST_UNSTAKE_DISCRIMINATOR);
}

export type RequestUnstakeInstruction<
  TProgram extends string = typeof STAKING_PROGRAM_ADDRESS,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOperator extends string
        ? ReadonlySignerAccount<TAccountOperator> & AccountSignerMeta<TAccountOperator>
        : TAccountOperator,
      TAccountPosition extends string ? WritableAccount<TAccountPosition> : TAccountPosition,
      TAccountConfig extends string ? ReadonlyAccount<TAccountConfig> : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type RequestUnstakeInstructionData = {
  discriminator: ReadonlyUint8Array;
  nodeId: bigint;
  amount: bigint;
};

export type RequestUnstakeInstructionDataArgs = {
  nodeId: number | bigint;
  amount: number | bigint;
};

export function getRequestUnstakeInstructionDataEncoder(): FixedSizeEncoder<RequestUnstakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['nodeId', getU64Encoder()],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_UNSTAKE_DISCRIMINATOR }),
  );
}

export function getRequestUnstakeInstructionDataDecoder(): FixedSizeDecoder<RequestUnstakeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['nodeId', getU64Decoder()],
    ['amount', getU64Decoder()],
  ]);
}

export function getRequestUnstakeInstructionDataCodec(): FixedSizeCodec<
  RequestUnstakeInstructionDataArgs,
  RequestUnstakeInstructionData
> {
  return combineCodec(
    getRequestUnstakeInstructionDataEncoder(),
    getRequestUnstakeInstructionDataDecoder(),
  );
}

export type RequestUnstakeAsyncInput<
  TAccountOperator extends string = string,
  TAccountPosition extends string = string,
  TAccountConfig extends string = string,
> = {
  operator: TransactionSigner<TAccountOperator>;
  position?: Address<TAccountPosition>;
  config?: Address<TAccountConfig>;
  nodeId: RequestUnstakeInstructionDataArgs['nodeId'];
  amount: RequestUnstakeInstructionDataArgs['amount'];
};

export async function getRequestUnstakeInstructionAsync<
  TAccountOperator extends string,
  TAccountPosition extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAKING_PROGRAM_ADDRESS,
>(
  input: RequestUnstakeAsyncInput<TAccountOperator, TAccountPosition, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RequestUnstakeInstruction<TProgramAddress, TAccountOperator, TAccountPosition, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    operator: { value: input.operator ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda({
      operator: getAddressFromResolvedInstructionAccount('operator', accounts.operator.value),
      nodeId: getNonNullResolvedInstructionInput('nodeId', args.nodeId),
    });
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('operator', accounts.operator),
      getAccountMeta('position', accounts.position),
      getAccountMeta('config', accounts.config),
    ],
    data: getRequestUnstakeInstructionDataEncoder().encode(
      args as RequestUnstakeInstructionDataArgs,
    ),
    programAddress,
  } as RequestUnstakeInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountPosition,
    TAccountConfig
  >);
}

export type RequestUnstakeInput<
  TAccountOperator extends string = string,
  TAccountPosition extends string = string,
  TAccountConfig extends string = string,
> = {
  operator: TransactionSigner<TAccountOperator>;
  position: Address<TAccountPosition>;
  config: Address<TAccountConfig>;
  nodeId: RequestUnstakeInstructionDataArgs['nodeId'];
  amount: RequestUnstakeInstructionDataArgs['amount'];
};

export function getRequestUnstakeInstruction<
  TAccountOperator extends string,
  TAccountPosition extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAKING_PROGRAM_ADDRESS,
>(
  input: RequestUnstakeInput<TAccountOperator, TAccountPosition, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): RequestUnstakeInstruction<TProgramAddress, TAccountOperator, TAccountPosition, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    operator: { value: input.operator ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('operator', accounts.operator),
      getAccountMeta('position', accounts.position),
      getAccountMeta('config', accounts.config),
    ],
    data: getRequestUnstakeInstructionDataEncoder().encode(
      args as RequestUnstakeInstructionDataArgs,
    ),
    programAddress,
  } as RequestUnstakeInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountPosition,
    TAccountConfig
  >);
}

export type ParsedRequestUnstakeInstruction<
  TProgram extends string = typeof STAKING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    operator: TAccountMetas[0];
    position: TAccountMetas[1];
    config: TAccountMetas[2];
  };
  data: RequestUnstakeInstructionData;
};

export function parseRequestUnstakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestUnstakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { operator: getNextAccount(), position: getNextAccount(), config: getNextAccount() },
    data: getRequestUnstakeInstructionDataDecoder().decode(instruction.data),
  };
}
