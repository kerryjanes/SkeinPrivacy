/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findConfigPda } from '../pdas';
import { REPUTATION_PROGRAM_ADDRESS } from '../programs';

export const SET_ORACLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  186, 128, 81, 104, 74, 79, 18, 224,
]);

export function getSetOracleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_ORACLE_DISCRIMINATOR);
}

export type SetOracleInstruction<
  TProgram extends string = typeof REPUTATION_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetOracleInstructionData = { discriminator: ReadonlyUint8Array; newOracle: Address };

export type SetOracleInstructionDataArgs = { newOracle: Address };

export function getSetOracleInstructionDataEncoder(): FixedSizeEncoder<SetOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newOracle', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ORACLE_DISCRIMINATOR }),
  );
}

export function getSetOracleInstructionDataDecoder(): FixedSizeDecoder<SetOracleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newOracle', getAddressDecoder()],
  ]);
}

export function getSetOracleInstructionDataCodec(): FixedSizeCodec<
  SetOracleInstructionDataArgs,
  SetOracleInstructionData
> {
  return combineCodec(getSetOracleInstructionDataEncoder(), getSetOracleInstructionDataDecoder());
}

export type SetOracleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  newOracle: SetOracleInstructionDataArgs['newOracle'];
};

export async function getSetOracleInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REPUTATION_PROGRAM_ADDRESS,
>(
  input: SetOracleAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<SetOracleInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>> {
  // Program address.
  const programAddress = config?.programAddress ?? REPUTATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('config', accounts.config),
    ],
    data: getSetOracleInstructionDataEncoder().encode(args as SetOracleInstructionDataArgs),
    programAddress,
  } as SetOracleInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>);
}

export type SetOracleInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  newOracle: SetOracleInstructionDataArgs['newOracle'];
};

export function getSetOracleInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REPUTATION_PROGRAM_ADDRESS,
>(
  input: SetOracleInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): SetOracleInstruction<TProgramAddress, TAccountAuthority, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? REPUTATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('config', accounts.config),
    ],
    data: getSetOracleInstructionDataEncoder().encode(args as SetOracleInstructionDataArgs),
    programAddress,
  } as SetOracleInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>);
}

export type ParsedSetOracleInstruction<
  TProgram extends string = typeof REPUTATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetOracleInstructionData;
};

export function parseSetOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getSetOracleInstructionDataDecoder().decode(instruction.data),
  };
}
