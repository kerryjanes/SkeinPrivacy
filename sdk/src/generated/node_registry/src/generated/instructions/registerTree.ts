/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findRegistryPda, findTreeShardPda } from '../pdas';
import { NODE_REGISTRY_PROGRAM_ADDRESS } from '../programs';

export const REGISTER_TREE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  22, 52, 192, 74, 201, 240, 87, 252,
]);

export function getRegisterTreeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REGISTER_TREE_DISCRIMINATOR);
}

export type RegisterTreeInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountMerkleTree extends string | AccountMeta<string> = string,
  TAccountTreeShard extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry,
      TAccountMerkleTree extends string ? ReadonlyAccount<TAccountMerkleTree> : TAccountMerkleTree,
      TAccountTreeShard extends string ? WritableAccount<TAccountTreeShard> : TAccountTreeShard,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterTreeInstructionData = {
  discriminator: ReadonlyUint8Array;
  index: number;
  maxDepth: number;
};

export type RegisterTreeInstructionDataArgs = { index: number; maxDepth: number };

export function getRegisterTreeInstructionDataEncoder(): FixedSizeEncoder<RegisterTreeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['index', getU16Encoder()],
      ['maxDepth', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_TREE_DISCRIMINATOR }),
  );
}

export function getRegisterTreeInstructionDataDecoder(): FixedSizeDecoder<RegisterTreeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['index', getU16Decoder()],
    ['maxDepth', getU32Decoder()],
  ]);
}

export function getRegisterTreeInstructionDataCodec(): FixedSizeCodec<
  RegisterTreeInstructionDataArgs,
  RegisterTreeInstructionData
> {
  return combineCodec(
    getRegisterTreeInstructionDataEncoder(),
    getRegisterTreeInstructionDataDecoder(),
  );
}

export type RegisterTreeAsyncInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountMerkleTree extends string = string,
  TAccountTreeShard extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry?: Address<TAccountRegistry>;
  merkleTree: Address<TAccountMerkleTree>;
  treeShard?: Address<TAccountTreeShard>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: RegisterTreeInstructionDataArgs['index'];
  maxDepth: RegisterTreeInstructionDataArgs['maxDepth'];
};

export async function getRegisterTreeInstructionAsync<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TAccountMerkleTree extends string,
  TAccountTreeShard extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterTreeAsyncInput<
    TAccountAuthority,
    TAccountRegistry,
    TAccountMerkleTree,
    TAccountTreeShard,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterTreeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountMerkleTree,
    TAccountTreeShard,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    merkleTree: { value: input.merkleTree ?? null, isWritable: false },
    treeShard: { value: input.treeShard ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda();
  }
  if (!accounts.treeShard.value) {
    accounts.treeShard.value = await findTreeShardPda({
      index: getNonNullResolvedInstructionInput('index', args.index),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('registry', accounts.registry),
      getAccountMeta('merkleTree', accounts.merkleTree),
      getAccountMeta('treeShard', accounts.treeShard),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRegisterTreeInstructionDataEncoder().encode(args as RegisterTreeInstructionDataArgs),
    programAddress,
  } as RegisterTreeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountMerkleTree,
    TAccountTreeShard,
    TAccountSystemProgram
  >);
}

export type RegisterTreeInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountMerkleTree extends string = string,
  TAccountTreeShard extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry: Address<TAccountRegistry>;
  merkleTree: Address<TAccountMerkleTree>;
  treeShard: Address<TAccountTreeShard>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: RegisterTreeInstructionDataArgs['index'];
  maxDepth: RegisterTreeInstructionDataArgs['maxDepth'];
};

export function getRegisterTreeInstruction<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TAccountMerkleTree extends string,
  TAccountTreeShard extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterTreeInput<
    TAccountAuthority,
    TAccountRegistry,
    TAccountMerkleTree,
    TAccountTreeShard,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterTreeInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountRegistry,
  TAccountMerkleTree,
  TAccountTreeShard,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    merkleTree: { value: input.merkleTree ?? null, isWritable: false },
    treeShard: { value: input.treeShard ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('registry', accounts.registry),
      getAccountMeta('merkleTree', accounts.merkleTree),
      getAccountMeta('treeShard', accounts.treeShard),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRegisterTreeInstructionDataEncoder().encode(args as RegisterTreeInstructionDataArgs),
    programAddress,
  } as RegisterTreeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountMerkleTree,
    TAccountTreeShard,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterTreeInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    registry: TAccountMetas[1];
    merkleTree: TAccountMetas[2];
    treeShard: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterTreeInstructionData;
};

export function parseRegisterTreeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterTreeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      registry: getNextAccount(),
      merkleTree: getNextAccount(),
      treeShard: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterTreeInstructionDataDecoder().decode(instruction.data),
  };
}
