/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findRegistryPda } from '../pdas';
import { NODE_REGISTRY_PROGRAM_ADDRESS } from '../programs';

export const SET_PAUSED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  91, 60, 125, 192, 176, 225, 166, 218,
]);

export function getSetPausedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_PAUSED_DISCRIMINATOR);
}

export type SetPausedInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type SetPausedInstructionData = { discriminator: ReadonlyUint8Array; paused: boolean };

export type SetPausedInstructionDataArgs = { paused: boolean };

export function getSetPausedInstructionDataEncoder(): FixedSizeEncoder<SetPausedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['paused', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PAUSED_DISCRIMINATOR }),
  );
}

export function getSetPausedInstructionDataDecoder(): FixedSizeDecoder<SetPausedInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['paused', getBooleanDecoder()],
  ]);
}

export function getSetPausedInstructionDataCodec(): FixedSizeCodec<
  SetPausedInstructionDataArgs,
  SetPausedInstructionData
> {
  return combineCodec(getSetPausedInstructionDataEncoder(), getSetPausedInstructionDataDecoder());
}

export type SetPausedAsyncInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry?: Address<TAccountRegistry>;
  paused: SetPausedInstructionDataArgs['paused'];
};

export async function getSetPausedInstructionAsync<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetPausedAsyncInput<TAccountAuthority, TAccountRegistry>,
  config?: { programAddress?: TProgramAddress },
): Promise<SetPausedInstruction<TProgramAddress, TAccountAuthority, TAccountRegistry>> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    registry: { value: input.registry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('registry', accounts.registry),
    ],
    data: getSetPausedInstructionDataEncoder().encode(args as SetPausedInstructionDataArgs),
    programAddress,
  } as SetPausedInstruction<TProgramAddress, TAccountAuthority, TAccountRegistry>);
}

export type SetPausedInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry: Address<TAccountRegistry>;
  paused: SetPausedInstructionDataArgs['paused'];
};

export function getSetPausedInstruction<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetPausedInput<TAccountAuthority, TAccountRegistry>,
  config?: { programAddress?: TProgramAddress },
): SetPausedInstruction<TProgramAddress, TAccountAuthority, TAccountRegistry> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    registry: { value: input.registry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('registry', accounts.registry),
    ],
    data: getSetPausedInstructionDataEncoder().encode(args as SetPausedInstructionDataArgs),
    programAddress,
  } as SetPausedInstruction<TProgramAddress, TAccountAuthority, TAccountRegistry>);
}

export type ParsedSetPausedInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    registry: TAccountMetas[1];
  };
  data: SetPausedInstructionData;
};

export function parseSetPausedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPausedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), registry: getNextAccount() },
    data: getSetPausedInstructionDataDecoder().decode(instruction.data),
  };
}
