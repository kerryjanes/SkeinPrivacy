/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findRegistryPda } from '../pdas';
import { NODE_REGISTRY_PROGRAM_ADDRESS } from '../programs';

export const SET_STAKE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  86, 173, 233, 196, 120, 133, 103, 254,
]);

export function getSetStakeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_STAKE_DISCRIMINATOR);
}

export type SetStakeInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountStakingAuthority extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountNode extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStakingAuthority extends string
        ? ReadonlySignerAccount<TAccountStakingAuthority> &
            AccountSignerMeta<TAccountStakingAuthority>
        : TAccountStakingAuthority,
      TAccountRegistry extends string ? ReadonlyAccount<TAccountRegistry> : TAccountRegistry,
      TAccountNode extends string ? WritableAccount<TAccountNode> : TAccountNode,
      ...TRemainingAccounts,
    ]
  >;

export type SetStakeInstructionData = { discriminator: ReadonlyUint8Array; amount: bigint };

export type SetStakeInstructionDataArgs = { amount: number | bigint };

export function getSetStakeInstructionDataEncoder(): FixedSizeEncoder<SetStakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_STAKE_DISCRIMINATOR }),
  );
}

export function getSetStakeInstructionDataDecoder(): FixedSizeDecoder<SetStakeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getSetStakeInstructionDataCodec(): FixedSizeCodec<
  SetStakeInstructionDataArgs,
  SetStakeInstructionData
> {
  return combineCodec(getSetStakeInstructionDataEncoder(), getSetStakeInstructionDataDecoder());
}

export type SetStakeAsyncInput<
  TAccountStakingAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountNode extends string = string,
> = {
  /** The staking program's `[b"authority"]` signer PDA (passed via CPI). */
  stakingAuthority: TransactionSigner<TAccountStakingAuthority>;
  registry?: Address<TAccountRegistry>;
  node: Address<TAccountNode>;
  amount: SetStakeInstructionDataArgs['amount'];
};

export async function getSetStakeInstructionAsync<
  TAccountStakingAuthority extends string,
  TAccountRegistry extends string,
  TAccountNode extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetStakeAsyncInput<TAccountStakingAuthority, TAccountRegistry, TAccountNode>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetStakeInstruction<TProgramAddress, TAccountStakingAuthority, TAccountRegistry, TAccountNode>
> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stakingAuthority: { value: input.stakingAuthority ?? null, isWritable: false },
    registry: { value: input.registry ?? null, isWritable: false },
    node: { value: input.node ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('stakingAuthority', accounts.stakingAuthority),
      getAccountMeta('registry', accounts.registry),
      getAccountMeta('node', accounts.node),
    ],
    data: getSetStakeInstructionDataEncoder().encode(args as SetStakeInstructionDataArgs),
    programAddress,
  } as SetStakeInstruction<
    TProgramAddress,
    TAccountStakingAuthority,
    TAccountRegistry,
    TAccountNode
  >);
}

export type SetStakeInput<
  TAccountStakingAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountNode extends string = string,
> = {
  /** The staking program's `[b"authority"]` signer PDA (passed via CPI). */
  stakingAuthority: TransactionSigner<TAccountStakingAuthority>;
  registry: Address<TAccountRegistry>;
  node: Address<TAccountNode>;
  amount: SetStakeInstructionDataArgs['amount'];
};

export function getSetStakeInstruction<
  TAccountStakingAuthority extends string,
  TAccountRegistry extends string,
  TAccountNode extends string,
  TProgramAddress extends Address = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetStakeInput<TAccountStakingAuthority, TAccountRegistry, TAccountNode>,
  config?: { programAddress?: TProgramAddress },
): SetStakeInstruction<TProgramAddress, TAccountStakingAuthority, TAccountRegistry, TAccountNode> {
  // Program address.
  const programAddress = config?.programAddress ?? NODE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stakingAuthority: { value: input.stakingAuthority ?? null, isWritable: false },
    registry: { value: input.registry ?? null, isWritable: false },
    node: { value: input.node ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('stakingAuthority', accounts.stakingAuthority),
      getAccountMeta('registry', accounts.registry),
      getAccountMeta('node', accounts.node),
    ],
    data: getSetStakeInstructionDataEncoder().encode(args as SetStakeInstructionDataArgs),
    programAddress,
  } as SetStakeInstruction<
    TProgramAddress,
    TAccountStakingAuthority,
    TAccountRegistry,
    TAccountNode
  >);
}

export type ParsedSetStakeInstruction<
  TProgram extends string = typeof NODE_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The staking program's `[b"authority"]` signer PDA (passed via CPI). */
    stakingAuthority: TAccountMetas[0];
    registry: TAccountMetas[1];
    node: TAccountMetas[2];
  };
  data: SetStakeInstructionData;
};

export function parseSetStakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetStakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      stakingAuthority: getNextAccount(),
      registry: getNextAccount(),
      node: getNextAccount(),
    },
    data: getSetStakeInstructionDataDecoder().decode(instruction.data),
  };
}
