/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findDistributorPda, findEpochDistributionPda } from '../pdas';
import { REWARDS_SETTLEMENT_PROGRAM_ADDRESS } from '../programs';

export const SWEEP_EPOCH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  187, 9, 52, 111, 60, 24, 52, 28,
]);

export function getSweepEpochDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SWEEP_EPOCH_DISCRIMINATOR);
}

export type SweepEpochInstruction<
  TProgram extends string = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountEpochDistribution extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountEpochDistribution extends string
        ? WritableAccount<TAccountEpochDistribution>
        : TAccountEpochDistribution,
      ...TRemainingAccounts,
    ]
  >;

export type SweepEpochInstructionData = { discriminator: ReadonlyUint8Array; epoch: bigint };

export type SweepEpochInstructionDataArgs = { epoch: number | bigint };

export function getSweepEpochInstructionDataEncoder(): FixedSizeEncoder<SweepEpochInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['epoch', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SWEEP_EPOCH_DISCRIMINATOR }),
  );
}

export function getSweepEpochInstructionDataDecoder(): FixedSizeDecoder<SweepEpochInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['epoch', getU64Decoder()],
  ]);
}

export function getSweepEpochInstructionDataCodec(): FixedSizeCodec<
  SweepEpochInstructionDataArgs,
  SweepEpochInstructionData
> {
  return combineCodec(getSweepEpochInstructionDataEncoder(), getSweepEpochInstructionDataDecoder());
}

export type SweepEpochAsyncInput<
  TAccountAuthority extends string = string,
  TAccountDistributor extends string = string,
  TAccountEpochDistribution extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  distributor?: Address<TAccountDistributor>;
  epochDistribution?: Address<TAccountEpochDistribution>;
  epoch: SweepEpochInstructionDataArgs['epoch'];
};

export async function getSweepEpochInstructionAsync<
  TAccountAuthority extends string,
  TAccountDistributor extends string,
  TAccountEpochDistribution extends string,
  TProgramAddress extends Address = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
>(
  input: SweepEpochAsyncInput<TAccountAuthority, TAccountDistributor, TAccountEpochDistribution>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SweepEpochInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDistributor,
    TAccountEpochDistribution
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDS_SETTLEMENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    distributor: { value: input.distributor ?? null, isWritable: true },
    epochDistribution: { value: input.epochDistribution ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await findDistributorPda();
  }
  if (!accounts.epochDistribution.value) {
    accounts.epochDistribution.value = await findEpochDistributionPda({
      epoch: getNonNullResolvedInstructionInput('epoch', args.epoch),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('epochDistribution', accounts.epochDistribution),
    ],
    data: getSweepEpochInstructionDataEncoder().encode(args as SweepEpochInstructionDataArgs),
    programAddress,
  } as SweepEpochInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDistributor,
    TAccountEpochDistribution
  >);
}

export type SweepEpochInput<
  TAccountAuthority extends string = string,
  TAccountDistributor extends string = string,
  TAccountEpochDistribution extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  distributor: Address<TAccountDistributor>;
  epochDistribution: Address<TAccountEpochDistribution>;
  epoch: SweepEpochInstructionDataArgs['epoch'];
};

export function getSweepEpochInstruction<
  TAccountAuthority extends string,
  TAccountDistributor extends string,
  TAccountEpochDistribution extends string,
  TProgramAddress extends Address = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
>(
  input: SweepEpochInput<TAccountAuthority, TAccountDistributor, TAccountEpochDistribution>,
  config?: { programAddress?: TProgramAddress },
): SweepEpochInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDistributor,
  TAccountEpochDistribution
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDS_SETTLEMENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    distributor: { value: input.distributor ?? null, isWritable: true },
    epochDistribution: { value: input.epochDistribution ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('epochDistribution', accounts.epochDistribution),
    ],
    data: getSweepEpochInstructionDataEncoder().encode(args as SweepEpochInstructionDataArgs),
    programAddress,
  } as SweepEpochInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDistributor,
    TAccountEpochDistribution
  >);
}

export type ParsedSweepEpochInstruction<
  TProgram extends string = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    distributor: TAccountMetas[1];
    epochDistribution: TAccountMetas[2];
  };
  data: SweepEpochInstructionData;
};

export function parseSweepEpochInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSweepEpochInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      distributor: getNextAccount(),
      epochDistribution: getNextAccount(),
    },
    data: getSweepEpochInstructionDataDecoder().decode(instruction.data),
  };
}
