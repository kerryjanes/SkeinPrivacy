/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findDistributorPda, findEpochDistributionPda } from '../pdas';
import { REWARDS_SETTLEMENT_PROGRAM_ADDRESS } from '../programs';

export const POST_EPOCH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  45, 8, 238, 205, 94, 1, 200, 51,
]);

export function getPostEpochDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POST_EPOCH_DISCRIMINATOR);
}

export type PostEpochInstruction<
  TProgram extends string = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
  TAccountPoster extends string | AccountMeta<string> = string,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountRewardVault extends string | AccountMeta<string> = string,
  TAccountEpochDistribution extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoster extends string
        ? WritableSignerAccount<TAccountPoster> & AccountSignerMeta<TAccountPoster>
        : TAccountPoster,
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountRewardVault extends string
        ? ReadonlyAccount<TAccountRewardVault>
        : TAccountRewardVault,
      TAccountEpochDistribution extends string
        ? WritableAccount<TAccountEpochDistribution>
        : TAccountEpochDistribution,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PostEpochInstructionData = {
  discriminator: ReadonlyUint8Array;
  epoch: bigint;
  merkleRoot: ReadonlyUint8Array;
  totalReward: bigint;
  numNodes: number;
};

export type PostEpochInstructionDataArgs = {
  epoch: number | bigint;
  merkleRoot: ReadonlyUint8Array;
  totalReward: number | bigint;
  numNodes: number;
};

export function getPostEpochInstructionDataEncoder(): FixedSizeEncoder<PostEpochInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['epoch', getU64Encoder()],
      ['merkleRoot', fixEncoderSize(getBytesEncoder(), 32)],
      ['totalReward', getU64Encoder()],
      ['numNodes', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POST_EPOCH_DISCRIMINATOR }),
  );
}

export function getPostEpochInstructionDataDecoder(): FixedSizeDecoder<PostEpochInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['epoch', getU64Decoder()],
    ['merkleRoot', fixDecoderSize(getBytesDecoder(), 32)],
    ['totalReward', getU64Decoder()],
    ['numNodes', getU32Decoder()],
  ]);
}

export function getPostEpochInstructionDataCodec(): FixedSizeCodec<
  PostEpochInstructionDataArgs,
  PostEpochInstructionData
> {
  return combineCodec(getPostEpochInstructionDataEncoder(), getPostEpochInstructionDataDecoder());
}

export type PostEpochAsyncInput<
  TAccountPoster extends string = string,
  TAccountDistributor extends string = string,
  TAccountRewardVault extends string = string,
  TAccountEpochDistribution extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poster: TransactionSigner<TAccountPoster>;
  distributor?: Address<TAccountDistributor>;
  rewardVault: Address<TAccountRewardVault>;
  epochDistribution?: Address<TAccountEpochDistribution>;
  systemProgram?: Address<TAccountSystemProgram>;
  epoch: PostEpochInstructionDataArgs['epoch'];
  merkleRoot: PostEpochInstructionDataArgs['merkleRoot'];
  totalReward: PostEpochInstructionDataArgs['totalReward'];
  numNodes: PostEpochInstructionDataArgs['numNodes'];
};

export async function getPostEpochInstructionAsync<
  TAccountPoster extends string,
  TAccountDistributor extends string,
  TAccountRewardVault extends string,
  TAccountEpochDistribution extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
>(
  input: PostEpochAsyncInput<
    TAccountPoster,
    TAccountDistributor,
    TAccountRewardVault,
    TAccountEpochDistribution,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PostEpochInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountDistributor,
    TAccountRewardVault,
    TAccountEpochDistribution,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDS_SETTLEMENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poster: { value: input.poster ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: true },
    rewardVault: { value: input.rewardVault ?? null, isWritable: false },
    epochDistribution: { value: input.epochDistribution ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await findDistributorPda();
  }
  if (!accounts.epochDistribution.value) {
    accounts.epochDistribution.value = await findEpochDistributionPda({
      epoch: getNonNullResolvedInstructionInput('epoch', args.epoch),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('poster', accounts.poster),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('rewardVault', accounts.rewardVault),
      getAccountMeta('epochDistribution', accounts.epochDistribution),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPostEpochInstructionDataEncoder().encode(args as PostEpochInstructionDataArgs),
    programAddress,
  } as PostEpochInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountDistributor,
    TAccountRewardVault,
    TAccountEpochDistribution,
    TAccountSystemProgram
  >);
}

export type PostEpochInput<
  TAccountPoster extends string = string,
  TAccountDistributor extends string = string,
  TAccountRewardVault extends string = string,
  TAccountEpochDistribution extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poster: TransactionSigner<TAccountPoster>;
  distributor: Address<TAccountDistributor>;
  rewardVault: Address<TAccountRewardVault>;
  epochDistribution: Address<TAccountEpochDistribution>;
  systemProgram?: Address<TAccountSystemProgram>;
  epoch: PostEpochInstructionDataArgs['epoch'];
  merkleRoot: PostEpochInstructionDataArgs['merkleRoot'];
  totalReward: PostEpochInstructionDataArgs['totalReward'];
  numNodes: PostEpochInstructionDataArgs['numNodes'];
};

export function getPostEpochInstruction<
  TAccountPoster extends string,
  TAccountDistributor extends string,
  TAccountRewardVault extends string,
  TAccountEpochDistribution extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
>(
  input: PostEpochInput<
    TAccountPoster,
    TAccountDistributor,
    TAccountRewardVault,
    TAccountEpochDistribution,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PostEpochInstruction<
  TProgramAddress,
  TAccountPoster,
  TAccountDistributor,
  TAccountRewardVault,
  TAccountEpochDistribution,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDS_SETTLEMENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poster: { value: input.poster ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: true },
    rewardVault: { value: input.rewardVault ?? null, isWritable: false },
    epochDistribution: { value: input.epochDistribution ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('poster', accounts.poster),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('rewardVault', accounts.rewardVault),
      getAccountMeta('epochDistribution', accounts.epochDistribution),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPostEpochInstructionDataEncoder().encode(args as PostEpochInstructionDataArgs),
    programAddress,
  } as PostEpochInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountDistributor,
    TAccountRewardVault,
    TAccountEpochDistribution,
    TAccountSystemProgram
  >);
}

export type ParsedPostEpochInstruction<
  TProgram extends string = typeof REWARDS_SETTLEMENT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poster: TAccountMetas[0];
    distributor: TAccountMetas[1];
    rewardVault: TAccountMetas[2];
    epochDistribution: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: PostEpochInstructionData;
};

export function parsePostEpochInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPostEpochInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      poster: getNextAccount(),
      distributor: getNextAccount(),
      rewardVault: getNextAccount(),
      epochDistribution: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPostEpochInstructionDataDecoder().decode(instruction.data),
  };
}
