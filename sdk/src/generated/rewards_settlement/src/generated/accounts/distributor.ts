/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const DISTRIBUTOR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  90, 90, 217, 147, 6, 32, 135, 4,
]);

export function getDistributorDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DISTRIBUTOR_DISCRIMINATOR);
}

export type Distributor = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  /** The off-chain aggregator that posts epoch roots. */
  posterAuthority: Address;
  /** Who may flag fraudulent leaves (→ slash + reputation penalty). */
  disputeAuthority: Address;
  rewardMint: Address;
  /** PDA token account = the node-reward pool (emissions + 70% of user payments). */
  rewardVault: Address;
  treasury: Address;
  disputeWindowSeconds: bigint;
  clawbackWindowSeconds: bigint;
  currentEpoch: bigint;
  /** Σ total_reward posted across all epochs. */
  cumulativeObligated: bigint;
  /** Σ claimed across all epochs. */
  cumulativeClaimed: bigint;
  bump: number;
};

export type DistributorArgs = {
  authority: Address;
  /** The off-chain aggregator that posts epoch roots. */
  posterAuthority: Address;
  /** Who may flag fraudulent leaves (→ slash + reputation penalty). */
  disputeAuthority: Address;
  rewardMint: Address;
  /** PDA token account = the node-reward pool (emissions + 70% of user payments). */
  rewardVault: Address;
  treasury: Address;
  disputeWindowSeconds: number | bigint;
  clawbackWindowSeconds: number | bigint;
  currentEpoch: number | bigint;
  /** Σ total_reward posted across all epochs. */
  cumulativeObligated: number | bigint;
  /** Σ claimed across all epochs. */
  cumulativeClaimed: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link DistributorArgs} account data. */
export function getDistributorEncoder(): FixedSizeEncoder<DistributorArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['posterAuthority', getAddressEncoder()],
      ['disputeAuthority', getAddressEncoder()],
      ['rewardMint', getAddressEncoder()],
      ['rewardVault', getAddressEncoder()],
      ['treasury', getAddressEncoder()],
      ['disputeWindowSeconds', getI64Encoder()],
      ['clawbackWindowSeconds', getI64Encoder()],
      ['currentEpoch', getU64Encoder()],
      ['cumulativeObligated', getU64Encoder()],
      ['cumulativeClaimed', getU64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DISTRIBUTOR_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Distributor} account data. */
export function getDistributorDecoder(): FixedSizeDecoder<Distributor> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['posterAuthority', getAddressDecoder()],
    ['disputeAuthority', getAddressDecoder()],
    ['rewardMint', getAddressDecoder()],
    ['rewardVault', getAddressDecoder()],
    ['treasury', getAddressDecoder()],
    ['disputeWindowSeconds', getI64Decoder()],
    ['clawbackWindowSeconds', getI64Decoder()],
    ['currentEpoch', getU64Decoder()],
    ['cumulativeObligated', getU64Decoder()],
    ['cumulativeClaimed', getU64Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Distributor} account data. */
export function getDistributorCodec(): FixedSizeCodec<DistributorArgs, Distributor> {
  return combineCodec(getDistributorEncoder(), getDistributorDecoder());
}

export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Distributor, TAddress>;
export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Distributor, TAddress>;
export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Distributor, TAddress> | MaybeAccount<Distributor, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getDistributorDecoder());
}

export async function fetchDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Distributor, TAddress>> {
  const maybeAccount = await fetchMaybeDistributor(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Distributor, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDistributor(maybeAccount);
}

export async function fetchAllDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Distributor>[]> {
  const maybeAccounts = await fetchAllMaybeDistributor(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Distributor>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDistributor(maybeAccount));
}

export function getDistributorSize(): number {
  return 241;
}
