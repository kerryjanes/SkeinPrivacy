/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const VESTING_SCHEDULE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  130, 200, 173, 148, 39, 75, 243, 147,
]);

export function getVestingScheduleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VESTING_SCHEDULE_DISCRIMINATOR);
}

export type VestingSchedule = {
  discriminator: ReadonlyUint8Array;
  /** Who may `claim` released tokens. */
  beneficiary: Address;
  /** Who may `revoke` (and receives the reclaimed unvested remainder). */
  authority: Address;
  /** The $WEFT mint this schedule vests. */
  mint: Address;
  /** The PDA-owned vault holding not-yet-claimed tokens. */
  vault: Address;
  /** Disambiguator allowing multiple schedules per beneficiary (PDA seed). */
  scheduleId: bigint;
  /** Total tokens deposited at creation (base units). Immutable. */
  totalAmount: bigint;
  /** Amount unlocked immediately at `start_ts` (TGE lump). 0 for all M1 schedules. */
  cliffUnlockAmount: bigint;
  /** Cumulative amount already transferred to the beneficiary. */
  releasedAmount: bigint;
  /** Unix timestamp vesting is measured from (TGE / genesis). */
  startTs: bigint;
  /** Seconds after `start_ts` before any linear amount unlocks. */
  cliffDuration: bigint;
  /** Total seconds from `start_ts` to fully vested. */
  duration: bigint;
  /** Whether `authority` may revoke and reclaim unvested tokens. */
  revocable: boolean;
  /** Set once revoked; freezes further accrual. */
  revoked: boolean;
  /** Timestamp accrual was frozen at (valid only when `revoked`). */
  revokedTs: bigint;
  /** PDA bump for signing vault CPIs. */
  bump: number;
};

export type VestingScheduleArgs = {
  /** Who may `claim` released tokens. */
  beneficiary: Address;
  /** Who may `revoke` (and receives the reclaimed unvested remainder). */
  authority: Address;
  /** The $WEFT mint this schedule vests. */
  mint: Address;
  /** The PDA-owned vault holding not-yet-claimed tokens. */
  vault: Address;
  /** Disambiguator allowing multiple schedules per beneficiary (PDA seed). */
  scheduleId: number | bigint;
  /** Total tokens deposited at creation (base units). Immutable. */
  totalAmount: number | bigint;
  /** Amount unlocked immediately at `start_ts` (TGE lump). 0 for all M1 schedules. */
  cliffUnlockAmount: number | bigint;
  /** Cumulative amount already transferred to the beneficiary. */
  releasedAmount: number | bigint;
  /** Unix timestamp vesting is measured from (TGE / genesis). */
  startTs: number | bigint;
  /** Seconds after `start_ts` before any linear amount unlocks. */
  cliffDuration: number | bigint;
  /** Total seconds from `start_ts` to fully vested. */
  duration: number | bigint;
  /** Whether `authority` may revoke and reclaim unvested tokens. */
  revocable: boolean;
  /** Set once revoked; freezes further accrual. */
  revoked: boolean;
  /** Timestamp accrual was frozen at (valid only when `revoked`). */
  revokedTs: number | bigint;
  /** PDA bump for signing vault CPIs. */
  bump: number;
};

/** Gets the encoder for {@link VestingScheduleArgs} account data. */
export function getVestingScheduleEncoder(): FixedSizeEncoder<VestingScheduleArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['beneficiary', getAddressEncoder()],
      ['authority', getAddressEncoder()],
      ['mint', getAddressEncoder()],
      ['vault', getAddressEncoder()],
      ['scheduleId', getU64Encoder()],
      ['totalAmount', getU64Encoder()],
      ['cliffUnlockAmount', getU64Encoder()],
      ['releasedAmount', getU64Encoder()],
      ['startTs', getI64Encoder()],
      ['cliffDuration', getI64Encoder()],
      ['duration', getI64Encoder()],
      ['revocable', getBooleanEncoder()],
      ['revoked', getBooleanEncoder()],
      ['revokedTs', getI64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VESTING_SCHEDULE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VestingSchedule} account data. */
export function getVestingScheduleDecoder(): FixedSizeDecoder<VestingSchedule> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['beneficiary', getAddressDecoder()],
    ['authority', getAddressDecoder()],
    ['mint', getAddressDecoder()],
    ['vault', getAddressDecoder()],
    ['scheduleId', getU64Decoder()],
    ['totalAmount', getU64Decoder()],
    ['cliffUnlockAmount', getU64Decoder()],
    ['releasedAmount', getU64Decoder()],
    ['startTs', getI64Decoder()],
    ['cliffDuration', getI64Decoder()],
    ['duration', getI64Decoder()],
    ['revocable', getBooleanDecoder()],
    ['revoked', getBooleanDecoder()],
    ['revokedTs', getI64Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VestingSchedule} account data. */
export function getVestingScheduleCodec(): FixedSizeCodec<VestingScheduleArgs, VestingSchedule> {
  return combineCodec(getVestingScheduleEncoder(), getVestingScheduleDecoder());
}

export function decodeVestingSchedule<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VestingSchedule, TAddress>;
export function decodeVestingSchedule<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VestingSchedule, TAddress>;
export function decodeVestingSchedule<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VestingSchedule, TAddress> | MaybeAccount<VestingSchedule, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVestingScheduleDecoder(),
  );
}

export async function fetchVestingSchedule<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VestingSchedule, TAddress>> {
  const maybeAccount = await fetchMaybeVestingSchedule(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVestingSchedule<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VestingSchedule, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVestingSchedule(maybeAccount);
}

export async function fetchAllVestingSchedule(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VestingSchedule>[]> {
  const maybeAccounts = await fetchAllMaybeVestingSchedule(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVestingSchedule(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VestingSchedule>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVestingSchedule(maybeAccount));
}

export function getVestingScheduleSize(): number {
  return 203;
}
