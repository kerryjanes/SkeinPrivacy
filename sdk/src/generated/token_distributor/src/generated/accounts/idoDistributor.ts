/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const IDO_DISTRIBUTOR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  232, 136, 45, 26, 254, 31, 254, 33,
]);

export function getIdoDistributorDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(IDO_DISTRIBUTOR_DISCRIMINATOR);
}

export type IdoDistributor = {
  discriminator: ReadonlyUint8Array;
  /** Governance authority (the DAO PDA after bootstrap) — may set the root pre-TGE. */
  authority: Address;
  mint: Address;
  /** PDA token account holding the full IDO allocation (TGE + vesting pool). */
  vault: Address;
  merkleRoot: ReadonlyUint8Array;
  /** Unix timestamp claiming opens. */
  tgeTs: bigint;
  /** Share released immediately at TGE (bps); the rest vests linearly. */
  tgeBps: number;
  /** Linear vesting duration for the non-TGE portion (seconds). */
  vestingDuration: bigint;
  /** Total allocation across the whole tree (for the solvency invariant). */
  totalAllocation: bigint;
  /** Σ claimed so far. */
  totalClaimed: bigint;
  bump: number;
};

export type IdoDistributorArgs = {
  /** Governance authority (the DAO PDA after bootstrap) — may set the root pre-TGE. */
  authority: Address;
  mint: Address;
  /** PDA token account holding the full IDO allocation (TGE + vesting pool). */
  vault: Address;
  merkleRoot: ReadonlyUint8Array;
  /** Unix timestamp claiming opens. */
  tgeTs: number | bigint;
  /** Share released immediately at TGE (bps); the rest vests linearly. */
  tgeBps: number;
  /** Linear vesting duration for the non-TGE portion (seconds). */
  vestingDuration: number | bigint;
  /** Total allocation across the whole tree (for the solvency invariant). */
  totalAllocation: number | bigint;
  /** Σ claimed so far. */
  totalClaimed: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link IdoDistributorArgs} account data. */
export function getIdoDistributorEncoder(): FixedSizeEncoder<IdoDistributorArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['mint', getAddressEncoder()],
      ['vault', getAddressEncoder()],
      ['merkleRoot', fixEncoderSize(getBytesEncoder(), 32)],
      ['tgeTs', getI64Encoder()],
      ['tgeBps', getU32Encoder()],
      ['vestingDuration', getI64Encoder()],
      ['totalAllocation', getU64Encoder()],
      ['totalClaimed', getU64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: IDO_DISTRIBUTOR_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link IdoDistributor} account data. */
export function getIdoDistributorDecoder(): FixedSizeDecoder<IdoDistributor> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['mint', getAddressDecoder()],
    ['vault', getAddressDecoder()],
    ['merkleRoot', fixDecoderSize(getBytesDecoder(), 32)],
    ['tgeTs', getI64Decoder()],
    ['tgeBps', getU32Decoder()],
    ['vestingDuration', getI64Decoder()],
    ['totalAllocation', getU64Decoder()],
    ['totalClaimed', getU64Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link IdoDistributor} account data. */
export function getIdoDistributorCodec(): FixedSizeCodec<IdoDistributorArgs, IdoDistributor> {
  return combineCodec(getIdoDistributorEncoder(), getIdoDistributorDecoder());
}

export function decodeIdoDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<IdoDistributor, TAddress>;
export function decodeIdoDistributor<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<IdoDistributor, TAddress>;
export function decodeIdoDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<IdoDistributor, TAddress> | MaybeAccount<IdoDistributor, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getIdoDistributorDecoder());
}

export async function fetchIdoDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<IdoDistributor, TAddress>> {
  const maybeAccount = await fetchMaybeIdoDistributor(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeIdoDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<IdoDistributor, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeIdoDistributor(maybeAccount);
}

export async function fetchAllIdoDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<IdoDistributor>[]> {
  const maybeAccounts = await fetchAllMaybeIdoDistributor(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeIdoDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<IdoDistributor>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeIdoDistributor(maybeAccount));
}

export function getIdoDistributorSize(): number {
  return 173;
}
