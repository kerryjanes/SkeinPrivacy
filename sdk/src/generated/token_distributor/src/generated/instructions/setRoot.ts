/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findDistributorPda } from '../pdas';
import { TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';

export const SET_ROOT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  183, 49, 10, 206, 168, 183, 131, 67,
]);

export function getSetRootDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_ROOT_DISCRIMINATOR);
}

export type SetRootInstruction<
  TProgram extends string = typeof TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      ...TRemainingAccounts,
    ]
  >;

export type SetRootInstructionData = {
  discriminator: ReadonlyUint8Array;
  merkleRoot: ReadonlyUint8Array;
  totalAllocation: bigint;
};

export type SetRootInstructionDataArgs = {
  merkleRoot: ReadonlyUint8Array;
  totalAllocation: number | bigint;
};

export function getSetRootInstructionDataEncoder(): FixedSizeEncoder<SetRootInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['merkleRoot', fixEncoderSize(getBytesEncoder(), 32)],
      ['totalAllocation', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ROOT_DISCRIMINATOR }),
  );
}

export function getSetRootInstructionDataDecoder(): FixedSizeDecoder<SetRootInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['merkleRoot', fixDecoderSize(getBytesDecoder(), 32)],
    ['totalAllocation', getU64Decoder()],
  ]);
}

export function getSetRootInstructionDataCodec(): FixedSizeCodec<
  SetRootInstructionDataArgs,
  SetRootInstructionData
> {
  return combineCodec(getSetRootInstructionDataEncoder(), getSetRootInstructionDataDecoder());
}

export type SetRootAsyncInput<
  TAccountAuthority extends string = string,
  TAccountDistributor extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  distributor?: Address<TAccountDistributor>;
  merkleRoot: SetRootInstructionDataArgs['merkleRoot'];
  totalAllocation: SetRootInstructionDataArgs['totalAllocation'];
};

export async function getSetRootInstructionAsync<
  TAccountAuthority extends string,
  TAccountDistributor extends string,
  TProgramAddress extends Address = typeof TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetRootAsyncInput<TAccountAuthority, TAccountDistributor>,
  config?: { programAddress?: TProgramAddress },
): Promise<SetRootInstruction<TProgramAddress, TAccountAuthority, TAccountDistributor>> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    distributor: { value: input.distributor ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await findDistributorPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('distributor', accounts.distributor),
    ],
    data: getSetRootInstructionDataEncoder().encode(args as SetRootInstructionDataArgs),
    programAddress,
  } as SetRootInstruction<TProgramAddress, TAccountAuthority, TAccountDistributor>);
}

export type SetRootInput<
  TAccountAuthority extends string = string,
  TAccountDistributor extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  distributor: Address<TAccountDistributor>;
  merkleRoot: SetRootInstructionDataArgs['merkleRoot'];
  totalAllocation: SetRootInstructionDataArgs['totalAllocation'];
};

export function getSetRootInstruction<
  TAccountAuthority extends string,
  TAccountDistributor extends string,
  TProgramAddress extends Address = typeof TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetRootInput<TAccountAuthority, TAccountDistributor>,
  config?: { programAddress?: TProgramAddress },
): SetRootInstruction<TProgramAddress, TAccountAuthority, TAccountDistributor> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    distributor: { value: input.distributor ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('distributor', accounts.distributor),
    ],
    data: getSetRootInstructionDataEncoder().encode(args as SetRootInstructionDataArgs),
    programAddress,
  } as SetRootInstruction<TProgramAddress, TAccountAuthority, TAccountDistributor>);
}

export type ParsedSetRootInstruction<
  TProgram extends string = typeof TOKEN_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    distributor: TAccountMetas[1];
  };
  data: SetRootInstructionData;
};

export function parseSetRootInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetRootInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), distributor: getNextAccount() },
    data: getSetRootInstructionDataDecoder().decode(instruction.data),
  };
}
