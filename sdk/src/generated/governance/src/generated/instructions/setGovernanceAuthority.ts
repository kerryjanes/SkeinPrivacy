/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGovernanceConfigPda } from '../pdas';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';

export const SET_GOVERNANCE_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  0, 113, 148, 253, 151, 181, 148, 42,
]);

export function getSetGovernanceAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_GOVERNANCE_AUTHORITY_DISCRIMINATOR);
}

export type SetGovernanceAuthorityInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGovernanceConfig extends string
        ? WritableAccount<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetGovernanceAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type SetGovernanceAuthorityInstructionDataArgs = { newAuthority: Address };

export function getSetGovernanceAuthorityInstructionDataEncoder(): FixedSizeEncoder<SetGovernanceAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newAuthority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GOVERNANCE_AUTHORITY_DISCRIMINATOR }),
  );
}

export function getSetGovernanceAuthorityInstructionDataDecoder(): FixedSizeDecoder<SetGovernanceAuthorityInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newAuthority', getAddressDecoder()],
  ]);
}

export function getSetGovernanceAuthorityInstructionDataCodec(): FixedSizeCodec<
  SetGovernanceAuthorityInstructionDataArgs,
  SetGovernanceAuthorityInstructionData
> {
  return combineCodec(
    getSetGovernanceAuthorityInstructionDataEncoder(),
    getSetGovernanceAuthorityInstructionDataDecoder(),
  );
}

export type SetGovernanceAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGovernanceConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  governanceConfig?: Address<TAccountGovernanceConfig>;
  newAuthority: SetGovernanceAuthorityInstructionDataArgs['newAuthority'];
};

export async function getSetGovernanceAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountGovernanceConfig extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetGovernanceAuthorityAsyncInput<TAccountAuthority, TAccountGovernanceConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetGovernanceAuthorityInstruction<TProgramAddress, TAccountAuthority, TAccountGovernanceConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    governanceConfig: { value: input.governanceConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.governanceConfig.value) {
    accounts.governanceConfig.value = await findGovernanceConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('governanceConfig', accounts.governanceConfig),
    ],
    data: getSetGovernanceAuthorityInstructionDataEncoder().encode(
      args as SetGovernanceAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetGovernanceAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGovernanceConfig
  >);
}

export type SetGovernanceAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountGovernanceConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  governanceConfig: Address<TAccountGovernanceConfig>;
  newAuthority: SetGovernanceAuthorityInstructionDataArgs['newAuthority'];
};

export function getSetGovernanceAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountGovernanceConfig extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetGovernanceAuthorityInput<TAccountAuthority, TAccountGovernanceConfig>,
  config?: { programAddress?: TProgramAddress },
): SetGovernanceAuthorityInstruction<TProgramAddress, TAccountAuthority, TAccountGovernanceConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    governanceConfig: { value: input.governanceConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('governanceConfig', accounts.governanceConfig),
    ],
    data: getSetGovernanceAuthorityInstructionDataEncoder().encode(
      args as SetGovernanceAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetGovernanceAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGovernanceConfig
  >);
}

export type ParsedSetGovernanceAuthorityInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    governanceConfig: TAccountMetas[1];
  };
  data: SetGovernanceAuthorityInstructionData;
};

export function parseSetGovernanceAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGovernanceAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), governanceConfig: getNextAccount() },
    data: getSetGovernanceAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
