/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findProtocolConfigPda } from '../pdas';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';

export const MIGRATE_PROTOCOL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  240, 133, 241, 218, 118, 253, 139, 28,
]);

export function getMigrateProtocolConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_PROTOCOL_CONFIG_DISCRIMINATOR);
}

export type MigrateProtocolConfigInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateProtocolConfigInstructionData = { discriminator: ReadonlyUint8Array };

export type MigrateProtocolConfigInstructionDataArgs = {};

export function getMigrateProtocolConfigInstructionDataEncoder(): FixedSizeEncoder<MigrateProtocolConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: MIGRATE_PROTOCOL_CONFIG_DISCRIMINATOR }),
  );
}

export function getMigrateProtocolConfigInstructionDataDecoder(): FixedSizeDecoder<MigrateProtocolConfigInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getMigrateProtocolConfigInstructionDataCodec(): FixedSizeCodec<
  MigrateProtocolConfigInstructionDataArgs,
  MigrateProtocolConfigInstructionData
> {
  return combineCodec(
    getMigrateProtocolConfigInstructionDataEncoder(),
    getMigrateProtocolConfigInstructionDataDecoder(),
  );
}

export type MigrateProtocolConfigAsyncInput<
  TAccountPayer extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays the rent for the extra bytes. */
  payer: TransactionSigner<TAccountPayer>;
  /** because the stored layout may be the shorter pre-M8 one. */
  protocolConfig?: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getMigrateProtocolConfigInstructionAsync<
  TAccountPayer extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: MigrateProtocolConfigAsyncInput<
    TAccountPayer,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MigrateProtocolConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('protocolConfig', accounts.protocolConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMigrateProtocolConfigInstructionDataEncoder().encode({}),
    programAddress,
  } as MigrateProtocolConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type MigrateProtocolConfigInput<
  TAccountPayer extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays the rent for the extra bytes. */
  payer: TransactionSigner<TAccountPayer>;
  /** because the stored layout may be the shorter pre-M8 one. */
  protocolConfig: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getMigrateProtocolConfigInstruction<
  TAccountPayer extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: MigrateProtocolConfigInput<TAccountPayer, TAccountProtocolConfig, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): MigrateProtocolConfigInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountProtocolConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('protocolConfig', accounts.protocolConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMigrateProtocolConfigInstructionDataEncoder().encode({}),
    programAddress,
  } as MigrateProtocolConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateProtocolConfigInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays the rent for the extra bytes. */
    payer: TAccountMetas[0];
    /** because the stored layout may be the shorter pre-M8 one. */
    protocolConfig: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: MigrateProtocolConfigInstructionData;
};

export function parseMigrateProtocolConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateProtocolConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      protocolConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateProtocolConfigInstructionDataDecoder().decode(instruction.data),
  };
}
